# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'LightStop' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!
  # Pods for LightStop
    pod 'Material', '~> 2.0'
    pod 'Alamofire', '~> 4.2.0'
    pod ‘SwiftyJSON’
    pod 'LiquidFloatingActionButton', :git => 'https://github.com/yoavlt/LiquidFloatingActionButton'

  target 'LightStopTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'LightStopUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
